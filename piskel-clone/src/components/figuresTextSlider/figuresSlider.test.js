import addFiguresSlider from './figuresSlider';

describe('addFiguresSlider', () => {
  it('Should be an instance of Function', () => {
    expect(addFiguresSlider).toBeInstanceOf(Function);
  });
});
